#include "graph.hpp"

namespace POLDAM
{
    OmniGraph computeDiffGraph(const OmniGraph &targetGraph)
    {
        OmniGraph diffGraph;

        // copy and paste from olda/diff.cpp
        return diffGraph;
    }

}